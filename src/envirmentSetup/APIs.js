const baseURL = 'http://http://localhost:9090/'

export const APIurl = {
    login: baseURL + 'login', //POST
    register: baseURL + 'signup', // POST
    Category: baseURL + 'categories', // GET PUT POST DELETE 
    task: baseURL + 'task', //
}